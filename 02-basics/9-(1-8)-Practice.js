





// JS  02-basics Practice 02/12/2026





// Arrays

//1. Create an array using all 4 methods ([], Array(), Array.of(), Array.from()) and log them.

//2. Push an array inside another array and prevent the original array from changing using concat and spread operator.

//3. Slice and splice the same array; log original and new arrays to show the difference.

//4. Flatten a nested array with depth 2 and Infinity.

//5. Convert an array to string with .join() using comma, space, and dash.

//6. Check if variable is an array using Array.isArray() and try Array.from() on string and object.

//7. Find index of a number that exists and one that doesn’t. Use .includes() to verify.

//8. Create a function that accepts any number of numbers and returns sum (rest operator).

//9. Objects

//10. Create an object with nested object and array inside object; log nested values.

//11. Add, modify, and delete properties dynamically in an object.

//12. Add a function inside object and access this to print object properties.

// Copy an object using spread operator and Object.assign(), then modify copy to show original is untouched.

// Use symbols as keys in an object and log them.

// Freeze an object and try modifying a property; log the result.

// Access object keys, values, and entries using Object.keys(), Object.values(), Object.entries().

// Destructure an object with renaming, nested destructuring, and arrays inside object.

// Functions

// Write a function with default parameters and test calling it without arguments.

// Create a function that takes an object as parameter and destructures it inside the function.

// Write a function that returns vs console.log and log results to see the difference.

// Pass an array to a function, modify it inside, and show how the original array changes.

// Extract first 3 elements of an array using destructuring inside a function.

// Use rest and spread operators in a function that multiplies all numbers passed.

// Scope & Hoisting

// Create a global variable and a local variable with same name; log them inside and outside function (shadowing).

// Log var, let, const variables before declaration to show hoisting/TDZ.

// Write a function expression and try calling it before declaration to see the trap.

// Arrow Functions & this

// Convert a normal function to an arrow function with implicit return.

// Write an arrow function returning an object; make sure to use parentheses.

// Create an object with normal method and arrow method; log this from both.

// IIFE

// Write a simple IIFE and log something.

// Write an IIFE with parameters and try naming it; see what happens if you call it later