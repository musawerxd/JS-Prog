


// JS Practice 02/15/2026



// Compare "5" and 5 using == and ===. Explain the difference.

// Check if 0, "0", false, null, undefined, NaN, and "" are truthy or falsy.

// Assign a variable username and default it to "Guest" using ||. Test with "", "0", null, and "Musawer".

// Use ?? to assign a default value only if a variable is null or undefined. Test with 0, "", and false.

// Write an if statement that prints "Adult" if age ≥ 18, otherwise "Minor". Then rewrite it using a ternary operator.

// Write a nested ternary to assign grades: ≥80 → A, ≥60 → B, ≥40 → C, <40 → Fail.

// Check if a number is between 10 and 20 using logical AND (&&).

// Print "Premium Access" only if both isPaid AND isVerified are true using &&.

// Check if a user is admin OR owner using logical OR (||). Print appropriate message.

// Use ! to reverse a boolean flag isLoggedIn. Print "Login required" if false.

// Compare "abc" and "bcd" using > and <. Print which comes first alphabetically.

// Write a switch for days of the week. Then remove breaks and explain fall-through.

// Use short-circuiting with AND: result = value1 && value2 && "Success". Test different combinations of truthy/falsy.

// Use short-circuiting with OR: result = value1 || value2 || "Default". Test 0, "", null, "Hello".

// Compare null and undefined using == and ===. Explain the difference.