




// JS  04-ControlFlow Practice 02/12/2026





// 1.

// Compare "5" and 5 using == and ===.Explain the difference.

// 2.

// Check if 0, "0", false, null, undefined, NaN, "" are truthy or falsy.

// 3.

// Use ?? to assign a default value only if a variable is null or undefined.Test with 0, "", and false.

// 4.

// Write an if that prints “Adult” if age ≥ 18, otherwise “Minor”. Then rewrite it using a ternary operator.

// 5.

// Write an if-else if-else chain for grades: ≥90 → A, ≥80 → B, ≥70 → C, ≥60 → D, else Fail.

// 6.

// Use a ternary operator to assign “Cheap” if price < 1000, else “Expensive”. Test with 0, 1000, and 1500.

// 7.

// Check if a number is between 10 and 20 using logical AND (&&).

// 8.

// Check if a user is admin OR owner using logical OR (||).Print appropriate message.

// 9.

// Use NOT(!) to reverse a boolean flag isLoggedIn.Print “Login required” if false.

// 10.

// Assign a username and default it to “Guest” using ||.Test with "", "0", null, and "Musawer".

// 11.

// Write a switch for days of the week.Then remove breaks and see what happens.Explain fall - through.

// 12.

// Write a switch using true to categorize marks into grades: ≥80 → A, ≥70 → B, ≥60 → C, else Fail.

// 13.

// Compare "abc" and "bcd" using > and <.Print which comes first alphabetically.

// 14.

// Check what happens if you compare null and undefined using == and ===.

// 15.

// Assign x = NaN and test its truthy / falsy behavior in an if statement.

// 16.

// Test short - circuiting with AND: result = value1 && value2 && "Success".Try different combinations of truthy / falsy.

// 17.

// Test short - circuiting with OR: result = value1 || value2 || "Default".Try 0, "", null, "Hello".

// 18.

// Write an if where you accidentally use = instead of ==.Explain what happens.

// 19.

// Compare "10" > 5 and "5" < 10. Explain type coercion.

// 20.

// Use nested ternary to assign grades: ≥80 → A, ≥60 → B, ≥40 → C, < 40 → Fail.

// 21.

// Check if a number is either < 0 or > 100 using logical OR.

// 22.

// Check if a string is empty or contains spaces only using truthy/falsy.

// 23.

// Use ?? to chain multiple variables: a ?? b ?? c.Explain which value is returned.

// 24.

// Print “Premium Access” only if both isPaid AND isVerified are true using &&.

// 25.

// Test what happens if you assign age = 18 inside if (age = 18).Compare to if (age === 18).

// 26.

// Assign price = 0 and use || and ?? to give a default. Compare outputs.

// 27.

// Compare true == 1 and true === 1. Explain why one is true and the other false.

// 28.

// Check the output of: Boolean([]), Boolean({}), Boolean("0"), Boolean("false").Explain why they are truthy.

// 29.

// Write a switch for a variable that can be 1–5, but put a default case first.Observe behavior.

// 30.

// Test a combination: 0 && null || "Fallback" ?? "Default".Print and explain the result.